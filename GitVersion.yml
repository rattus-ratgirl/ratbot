mode: ContinuousDeployment
commit-message-incrementing: Enabled
strategies:
  - Mainline
  - ConfiguredNextVersion
  - MergeMessage
  - TaggedCommit
branches:
  main:
    increment: Patch
    regex: ^(main|master)$
    is-main-branch: true